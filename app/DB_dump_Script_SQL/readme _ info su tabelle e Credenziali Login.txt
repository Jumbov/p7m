Lo schema check-files-db è composto da 3 tabelle:

- 2 tabelle (users, authorities) per i dati dell'utente e il suo ruolo;
- 1 tabella (extentions_mime) per l'elenco dei tipi di estensione di file contemplati.

USER: andrea
PASSWORD DECRIPTATA: a9010